; https://www.jobify.live/
; ------------ Git Crash Course by Brad Traversy -----------
git --version
git init
git config --global user.name "Walusimbi Herbert Henry"
git config --global user.email "walherb@gmail"
git add -A
git status
git rm --cached index.html
git add *.html
git add . // Add Everything
git commit -m "Comments or Desc Here..."

git branch login
git checkout login  // "Make changes and Commit then go back"
git checkout master
git merge login

git remote

git remote add origin ..(url).git
git remote
git push -u origin master

; Add Some Files then push again
git push

; Pull Remote repository
git clone ..(url).git

; To pull Everything if there were multiple developers
git pull

; -------------- cd myapp ------------------
git init
git config --global user.name "Walusimbi Herbert Henry"
git config --global user.email "walherb@gmail.com"
git add index.html
git status
git rm --cached index.html
git add *.html
git add .
git commit -m "Changed app.js"

touch .gitignore

; Walusimbi Herbert Henry
; Create a repository on github web
git remote add origin https://github.com/walherb/myappsample.git

git remote
git push -u origin master

git ls-remote
; git remote add origin-push https://github.com/walherb/mysampleapp.git
git remote add origin https://github.com/walherb/myappsample.git
git remote remove origin
git remote remove origin-push
git push -u origin main

git show-ref
git push origin HEAD:<branch>

git checkout main

; Solving errors
git init
; git remote add origin https://github.com/walherb/myappsample.git
git branch -M main
git add .
git commit -m 'feat: Solving Errors'
git commit -m 'feat: Brad Traversy'
git remote add origin https://github.com/walherb/bradtraversy.git
git push -u origin main

git push -u origin main --force ; { Solved !: ! [rejected] main -> main (fetch first) }

; https://stackoverflow.com/questions/28429819/rejected-master-master-fetch-first
; Don't Initialize repo with README file in GitHub remote to avoid this error
git init
git add README.md
git add .
git commit -m "first commit"
git remote add origin https://github.com/userName/repoName.git
git push --force origin master

git fetch origin main
git pull origin main
git add .
git commit -m ""
git push origin main

git pull origin master --allow-unrelated-histories
git push -u origin master

; Advised solution
git pull --rebase origin master
git push -u origin maste

git push -f origin main

; ---------- Github Tutorial For Beginners - Github Basics for Mac or Windows & Source Control Basics {5*s} ---
git config --global user.name "Walusimbi Herbert Henry"
git config --global user.email "walherb@gmail.com"
git clone https://github.com/walherb/newGitTest.git
git status
git add index.html
git add index2.html
git commit -m "Added index.html"
git commit -m "Added index2.html"
git push        ; for first user
git pull        ; with another user
git status      ; after making some chnages to index.html
git add index.html
git status
git add A
git commit -m "Made Changes"
git push        ; another user
git pull        ; first user


; --------- Corey Schafer Git Tutorial --------------
git --version

git config --global user.name "Walusimbi Herbert Henry"
git config --global user.email "walherb@gmail.com"

git config --list

git help <help>
git <verb> --help

git help config
git config --help

; --- Initialize a Repository From Existing Code ---
git init

; --- Before First Commit ---
git status

; --- Ignore Files ---
touch .gitignore
; -- Add
.DS_Store
.project
*.pyc

; --- Add Files To Staging Area ---
git add -A
git status

; e.g
git add .gitignore
git status

; --- Remove Files From Staging Area ---
git reset calc.py
git reset

; ---------------- Our First Commit -------------------
git add -A
git commit -m "Initial Commit"
git log

; ------------------ Cloning A Remote Repo ------------
git clone <url> <where to clone>
git clone ../remote_repo.git .
git clone https://github.com/CoreyMSchafer/remote_repo.git .


; -- Viewing Information ABout The Remote Repository -----
git remote -v
git branch -a

; --- Pushing Changes ---------
; -- Commit Changes Like We Did Previously --
git diff
git status
git add -A
git commit -m "Modified Multiply Function"

; --- Then Push --
git pull origin master
git push origin master

; ------- Common Workflow ---------------
; --- Create A Branch For Desired Future --
git branch calc-divide
git branch checkout calc-divide

git status
git add -A
git commit -m "Divide Function"

; --- After Commit Push Branch To Remote ---
git push -u origin calc-divide
git branch -a

; ----- Merge A Branch -----
git checkout master
git pull origin master
git branch --merged
git merge calc-divide
git push origin master

; --- Deleting A Branch ---
git branch --merged
git branch -d calc-divide
git branch -a
git push origin --delete calc-divide

; --- Faster Example ---
git branch subtract
git checkout subtract
git status
git add -A
git commit -m "Subtract"
git push -u origin subtract
git checkout master
git pull origin master
git merge subtract
git push origin master


; ------------ Mosh Hamedani ----------------------------
git --version
git config --global user.name "Walusimbi Herbert Henry"
git config --global user.email walherb@gmail.com
git config --global core.editor "code --wait"
git config --global -e
git config --global core.autocrlf true
git config --help

; -------------- Creating Snapshots ------------
git add .
git status
git commit -m "Initial Commit"

git commit -a -m "Add and Commit at the same time."

git ls-files
git rm *.txt

git mv main.js file.js

.gitignore

git rm -h
git rm --cached -r bin/

git status -s

git diff --staged   ; see staged changes
git diff ; see unstaged changes


git config --global diff.tool vscode
git config --global difftool.vscode.cmd "code --wait --diff $LOCAL $REMOTE"

git log
git log --oneline
git log --oneline --reverse
git show d601
git show HEAD~1
git show HEAD~1:.gitignore
git ls-tree
git ls-tree HEAD~1

git restore --staged file.js

git clean -fd

git restore --source=HEAD~1










